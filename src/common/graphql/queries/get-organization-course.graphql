query GetOrganizationCourse($courseId: String!) {
    getOrganizationCourse(courseId: $courseId) {
        id
        avatar_url
        currency
        description
        domains
        instructor {
            id
            email
            name
        }
        level
        price
        title
        is_subscribed
        is_course_in_cart
    }
}
