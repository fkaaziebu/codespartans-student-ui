query ListStudentSubscriptions {
  listMyStudentSubscriptions {
    id
    status
    started_at
    expires_at
    plan {
      id
      name
      price
      currency
      interval
      duration_days
    }
  }
}
