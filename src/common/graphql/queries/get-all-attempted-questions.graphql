query GetAllAttemptedQuestions($testId: String!) {
    getAllAttemptedQuestions(testId: $testId) {
        answer_provided
        question_id
        is_flagged
        is_correct
        is_marked
        question {
            type
        }
    }
}
