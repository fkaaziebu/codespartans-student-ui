query LoginStudent($email: String!, $password: String!) {
    loginStudent(email: $email, password: $password) {
        id
        email
        name
        token
        organizations {
            id
        }
    }
}
