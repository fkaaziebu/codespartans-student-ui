query StudentProfile {
    studentProfile {
        id
        name
        email
        is_setup_completed
        organizations {
            id
            email
        }
        subscribed_categories {
            id
            name
        }
    }
}
