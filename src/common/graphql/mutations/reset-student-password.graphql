mutation ResetStudentPassword(
    $email: String!
    $token: String!
    $password: String!
) {
    resetStudentPassword(email: $email, token: $token, password: $password) {
        message
    }
}
