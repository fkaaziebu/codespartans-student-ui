mutation RegisterStudent(
    $name: String!
    $email: String!
    $password: String!
    $organizationId: String!
) {
    registerStudent(
        name: $name
        email: $email
        password: $password
        organizationId: $organizationId
    ) {
        message
    }
}
